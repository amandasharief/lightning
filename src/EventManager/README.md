# Event Manager (PSR-14: Event Dispatcher)

Provides the `EventManagerInterface` and a ultralightweight `EventManager` implementation. The `EventManagerInterface` will be used by some of our other componenents.
